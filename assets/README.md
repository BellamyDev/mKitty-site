# Site Assets

This folder holds images, icons, and other static assets referenced by the site.

Suggested files to add:
- `og-image.png` (1200×630) — preview when the site is shared on social/chat
- `favicon.svg` — browser tab icon
- `logo/` — when you commission a real mKitty logo, drop the SVG/PNG variants here
